#!/usr/bin/env bash

# it is recommended to use #!/usr/bin/env bash as it allows for more flexibility in finding the correct interpreter. It ensures that the script will work even if the bash interpreter is located in a different directory or if different systems have a different path to the bash interpreter

BACKUP_DIR_PATH='/backups'
BACKUP_FILE_PREFIX='backup'